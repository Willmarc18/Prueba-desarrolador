<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>TABLA CUENTAS</title>
    <!-- NAVIGATION -->
    <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
  </head>
  <body>

    <!-- navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <a class="navbar-brand" href="/">CUENTAS</a>
    </nav>

        <div class="container mt-5">
            <div class="row">
                <div class="col-md-5">
                    <div class="card">
                        <div class="card-body">
                            <form action="/update/<%= data.id%>" method="POST">
                                <div class="form-group">
                                    <input type="text" name="nombres" value="<%= data.nombres%>" placeholder="Nombres" class="form-control">
                                </div>
                                <div class="form-group">
                                    <input type="text" name="apellidos" value="<%= data.apellidos%>" placeholder="Apellidos" class="form-control">
                                </div>
                                <div class="form-group">
                                    <input type="text" name="numero_cuenta" value="<%= data.numero_cuenta%>" placeholder="Numero cuenta" class="form-control">
                                </div>

                                <button type="button" class="btn btn-primary" onclick="alerta()">
                                    ACTUALIZAR
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>

</body>
</html>